#:kivy 1.8.0
#:import datetime datetime

RootWidget:
    # import container
    container: container

    # fill the container
    BoxLayout:
        id: container
        orientation: 'vertical'
        padding: 0
        spacing: 3

        Label:
            text: 'screen-2'

        Button:
            size_hint: 0.2, 1
            text: 'CH1'

        Button:
            size_hint: 0.2, 1
            text: 'CH2'

        Button:
            size_hint: 0.2, 1
            text: 'CH3'

        Button:
            size_hint: 0.2, 1
            text: 'CH4'

        Button:
            size_hint: 0.2, 1
            text: 'CH5'

        Button:
            size_hint: 0.2, 1
            text: 'CH6'

        Button:
            size_hint: 0.2, 1
            text: 'CH7'

        Button:
            size_hint: 0.2, 1
            text: 'CH8'

        BoxLayout:
            orientation: 'horizontal'
            padding: 0
            spacing: 1
            size_hint: 1, 0.1

            # weiter button
            Button:
                size_hint: 0.2, 1
                text: 'next'
                on_release: app.next_screen('3')

